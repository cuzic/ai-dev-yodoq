{
  "name": "ai-dev-yodoq",
  "version": "1.0.0",
  "description": "AI活用開発セミナー：生産性を劇的に向上させる体系的アプローチ",
  "private": true,
  "scripts": {
    "build": "npm run build:pdf && npm run build:html",
    "build:pdf": "mkdir -p build && marp slides/*.md --pdf --allow-local-files && mv slides/*.pdf build/",
    "build:html": "mkdir -p build && marp slides/*.md --html --allow-local-files && mv slides/*.html build/",
    "build:pptx": "mkdir -p build && marp slides/*.md --pptx --allow-local-files && mv slides/*.pptx build/",
    "preview": "marp -s slides/",
    "watch": "marp -w slides/day1_1.md"
  },
  "keywords": [
    "marp",
    "presentation",
    "slides",
    "ai",
    "development",
    "seminar"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.0.0",
    "@playwright/test": "^1.56.1"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
